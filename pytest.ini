[pytest]
pythonpath = .
testpaths = test
asyncio_mode = auto
# One event loop for the whole run: the session-scoped engine fixture would
# otherwise be bound to a loop that is closed before the tests using it run.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    integration: requires the local Postgres container
